# Контекст сессии Equilibrium

Этот файл служит "внешней памятью" для AI-ассистента. Обновляй его в конце сессии или при смене контекста.

## 🟢 Текущий статус (Active Focus)
**Дата:** 2026-02-02
**Приоритет:** Отладка Python-скриптов и оформление документации.

### 1. Скрипт сортировки почты (`e-mail-sorter-v4.2.py`)
- **Статус:** В процессе отладки.
- **Последние изменения:** Исправлена ошибка с проверкой пустых строк при предпросмотре Excel (`if row:`).
- **Проблема:** Скрипт падал или некорректно отображал данные на пустых строках.
- **Задача:** Проверить, остались ли еще уязвимые места при чтении `openpyxl`.

### 2. Лог сборки ЧПУ (`cnc-build-log.md`)
- **Статус:** Требует рефакторинга.
- **Проблемы:**
    - Смешанные ссылки на картинки (Obsidian `![[]]` и Web `!`).
    - Артефакты форматирования (лишние `*`, пустые блоки кода).
    - Странные заголовки.
- **Задача:** Причесать Markdown под стандарт Obsidian.

### 3. Статья про локальные LLM (`top-6-local-models.md`)
- **Статус:** Готовый материал.
- **Задача:** Интегрировать в структуру знаний (возможно, создать ссылки из `how-to-start-ai.md`).

## 📝 Архитектурные заметки
- Используем абсолютные пути в скриптах аккуратно, лучше переходить на относительные внутри `Projects/`.
- Все задачи фиксируем тегом `#task` для Obsidian Tasks.