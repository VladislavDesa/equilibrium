# Задача: Редактирование атрибутов через VEX

> Уровень: 🟠 Средний  
> Контекст: SOP → Attribute Wrangle  
> Цель: Научиться управлять геометрией и визуализацией через код.

---

## 🎯 Что нужно сделать

1. Создайте **Grid** (20×20 divisions).
2. Добавьте **Mountain** → добавьте базовый рельеф.
3. Добавьте **Attribute Wrangle** (Run Over: **Points**).
4. Напишите код, который:
   - смещает вершины по Z с помощью **3D-шума**,
   - окрашивает точки по высоте (`@P.y` → `@Cd`),
   - устанавливает `@pscale` (размер точки) случайно.
5. Добавьте второй **Attribute Wrangle** (Run Over: **Detail**):
   - выведите в консоль количество точек:  
     ```c
     printf("Total points: %d\n", npoints(0));
     ```
6. Добавьте **Sphere** и используйте **Point Wrangle**, чтобы:
   - удалить все точки выше Y=0.5:  
     ```c
     if (@P.y > 0.5) removepoint(0, @ptnum);
     ```
7. Сохраните сцену как `solution.hipnc`.

---

## 💡 Подсказки

- Используйте `Ctrl+C` в Viewport, чтобы скопировать координаты точки.
- Если геометрия исчезла — проверьте, не удалили ли вы всё!
- Для шума: `@P += noise(@P * 3) * 0.1;`

---

## ✅ Критерии завершения

- [ ] Файл `.hipnc` сохранён в папке задания
- [ ] Все шаги выполнены
- [ ] Заметки добавлены в `notes.md`
- [ ] Прогресс обновлён в:
  - [ ] `Progress/vlad-progress.md`
  - [ ] `Progress/alexander-progress.md`
- [ ] (Опционально) Сделан скриншот результата с цветовой картой высот